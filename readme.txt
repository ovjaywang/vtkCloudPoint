0.1 完成dbscan算法聚类 完成树形的添加 点云的可视化
0.2　添加树形文件夹多文件的加载　勾选　完成电机坐标到三维坐标的转换
0.4　完成质心点的添加 完成聚类、质心显示、匹配的分离 完成部分icp匹配 完成点云不同类别点的显示
0.6 完成匹配成功与否的显示 完成质心点的输出
1.0 完善dll的调用 完善菜单栏（待优化工作 进度条的显示 左侧树形菜单的勾选同步 匹配结果的全部显示 exe文件的调用）
1.1 完善计算时的进度条 完善icp的旋转 完善树形结构
1.4 添加点时加入选择框
1.5　改进了大部分数据结构　更加节省内存　改进的treeview的bug 改进了单位和提示 后期改进中心显示和匹配显示
1.7  修正了质心算法 修正了质心点的显示 修正匹配坐标 修正所有输出文件 顺滑爽快
	-后期改进输出图片、xls输入、扫描点文件聚类、最小外接圆 同数据剔除
1.8  加入了所有能用的xls。但效率依旧很慢
     加入了最小外界圆和外接矩形。窗体选择等待加入
     可显示矩形和圆形。是否剔除等待加入
1.9 加入窗体选择和过滤确认点。
    修改了一些细节（窗体变化时刷新 输入参数错误判断 逻辑enabled判断）
    等待加入选点、稳定的ICP。
1.10 发现外接圆算法不稳定性 重新写入外接圆
     添加point的match坐标属性 修改获得点集边界的算法
     修改ICP结果显示。可以按输入确认是否匹配
     等待加入选点。等待加入KDTree
1.11 添加辅助视觉坐标系 有助于提高分辨
     添加时钟旋转和、 但坐标方位有待测试
     添加坐标系失败
     画连线修正成功 
     等待加入kdtree 等待修正坐标问题 等待写入匹配后图案
1.12 辅助坐标修正
     匹配结果连线及点的显示修正
     外接圆外接矩形重复显示修正
     坐标旋转、坐标反转功能实现
     匹配阈值可修改
     待加入kdtree和速度
1.13 加入kdtree失败 
     0.06 6依然不能适配
     加入截屏
     加入重复点判断
1.14 改善了扫描点的所有功能
     尝试加入point pick。有失败几率很大。
1.15 加入了棋盘匹配 效率高 但需要手动更多的输入
     修改了进入界面的鼠标点击方式
     后期加入源文件输入匹配及匹配时上下移动
1.16 加入源文件匹配 直接输出聚类和匹配 
     将xls和txt融合
     加入更多条件输入及判断窗口
     加入更多错误判断
     后期纠正你匹配的结果精度问题及快捷键位的问题
1.17 将半径改为x y步长
     将源文件匹配错误修正
     将匹配时坐标转换修改得更为准确 使匹配迭代次数少 不易崩